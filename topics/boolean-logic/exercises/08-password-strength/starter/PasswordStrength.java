public class PasswordStrength {

    /**
     * Returns true if the password has at least 8 characters.
     */
    public static boolean isLongEnough(String password) {
        // TODO: Check password length
        return false;
    }

    /**
     * Returns true if the password contains at least one digit ('0'-'9').
     */
    public static boolean hasDigit(String password) {
        // TODO: Loop through characters and check for digits
        return false;
    }

    /**
     * Returns true if the password contains at least one uppercase letter ('A'-'Z').
     */
    public static boolean hasUppercase(String password) {
        // TODO: Loop through characters and check for uppercase
        return false;
    }

    /**
     * Returns true if the password is long enough, has a digit, and has an uppercase letter.
     */
    public static boolean isStrong(String password) {
        // TODO: Combine the three checks above
        return false;
    }
}

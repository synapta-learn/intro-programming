public class InRange {

    /**
     * Returns true if n is between 1 and 100 (inclusive).
     *
     * @param n the number to check
     * @return true if 1 <= n <= 100
     */
    public static boolean inRange(int n) {
        // TODO: Return true if n is between 1 and 100 (inclusive)
        return false;
    }
}

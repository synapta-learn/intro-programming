public class Solution {

    /**
     * Returns true if the three sides can form a valid triangle.
     * All sides must be positive, and the sum of any two sides
     * must be strictly greater than the third side.
     *
     * @param a first side length
     * @param b second side length
     * @param c third side length
     * @return true if a valid triangle can be formed
     */
    public static boolean isValidTriangle(int a, int b, int c) {
        // TODO: Check that all sides are positive and satisfy the triangle inequality
        return false;
    }
}

public class DeMorgans {

    /**
     * Equivalent to !(a && b).
     * Implement using De Morgan's law: !a || !b
     */
    public static boolean notBoth(boolean a, boolean b) {
        // TODO: Implement using !a || !b
        return false;
    }

    /**
     * Equivalent to !(a || b).
     * Implement using De Morgan's law: !a && !b
     */
    public static boolean notEither(boolean a, boolean b) {
        // TODO: Implement using !a && !b
        return false;
    }

    /**
     * Equivalent to !(a && b && c).
     * Implement using De Morgan's law: !a || !b || !c
     */
    public static boolean notAllThree(boolean a, boolean b, boolean c) {
        // TODO: Implement using !a || !b || !c
        return false;
    }
}

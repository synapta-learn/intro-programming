public class CanVote {

    /**
     * Returns true if the person is eligible to vote.
     * A person can vote if they are at least 18 years old AND a citizen.
     *
     * @param age the person's age
     * @param isCitizen whether the person is a citizen
     * @return true if eligible to vote
     */
    public static boolean canVote(int age, boolean isCitizen) {
        // TODO: Return true if age >= 18 AND isCitizen is true
        return false;
    }
}

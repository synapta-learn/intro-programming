public class XorWithoutXor {

    /**
     * Returns the XOR of a and b, using only &&, ||, and !.
     * Do NOT use the ^ operator.
     *
     * @param a first boolean
     * @param b second boolean
     * @return true if exactly one of a or b is true
     */
    public static boolean xor(boolean a, boolean b) {
        // TODO: Implement XOR using only &&, ||, and !
        return false;
    }
}

# Is Positive

## Problem

Write a method that returns `true` if the given number is positive (greater than zero), and `false` otherwise.

## Examples

```
isPositive(5)   → true
isPositive(-3)  → false
isPositive(0)   → false
```

## Hint

A number is positive if it is strictly greater than zero. Zero itself is neither positive nor negative.

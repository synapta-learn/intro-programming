public class Quadrant {

    /**
     * Returns the quadrant or special location of point (x, y).
     * "Origin" if both are 0, "Axis" if on an axis,
     * otherwise "Q1", "Q2", "Q3", or "Q4".
     *
     * @param x the x coordinate
     * @param y the y coordinate
     * @return the location string
     */
    public static String quadrant(int x, int y) {
        // TODO: Determine the location of the point
        return "";
    }
}

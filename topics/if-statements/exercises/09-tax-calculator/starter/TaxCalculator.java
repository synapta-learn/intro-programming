public class TaxCalculator {

    /**
     * Calculates progressive income tax.
     * $0-$10,000 at 10%, $10,001-$40,000 at 20%, $40,001+ at 30%.
     * Returns 0 if income is 0 or negative.
     *
     * @param income the total income
     * @return the total tax owed
     */
    public static double calculateTax(double income) {
        // TODO: Calculate tax using progressive brackets
        return 0;
    }
}

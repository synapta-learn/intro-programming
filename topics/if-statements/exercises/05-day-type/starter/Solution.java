public class Solution {

    /**
     * Returns "Weekday" for days 1-5, "Weekend" for days 6-7,
     * or "Invalid" for any other number.
     * Days: 1=Monday, 2=Tuesday, ..., 7=Sunday
     *
     * @param day the day number (1-7)
     * @return "Weekday", "Weekend", or "Invalid"
     */
    public static String dayType(int day) {
        // TODO: Use a switch statement to classify the day
        return "";
    }
}

public class Solution {

    public static int toHours(int totalSeconds) {
        // TODO: Return the number of complete hours
        return 0;
    }

    public static int toMinutes(int totalSeconds) {
        // TODO: Return the number of remaining minutes (after removing hours)
        return 0;
    }

    public static int toSeconds(int totalSeconds) {
        // TODO: Return the number of remaining seconds (after removing full minutes)
        return 0;
    }

    public static void main(String[] args) {
        int total = 3661;
        System.out.println(total + " seconds is:");
        System.out.println(toHours(total) + " hours");
        System.out.println(toMinutes(total) + " minutes");
        System.out.println(toSeconds(total) + " seconds");
    }
}

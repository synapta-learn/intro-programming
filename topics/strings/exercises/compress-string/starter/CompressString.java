public class CompressString {

    /**
     * Compresses the string using counts of consecutive repeated characters.
     * Returns the original string if compression does not make it shorter.
     *
     * Example: "aabcccccaaa" becomes "a2b1c5a3"
     *
     * @param text the input string
     * @return the compressed string, or the original if compression is not shorter
     */
    public static String compress(String text) {
        // TODO: Compress the string using character counts
        return "";
    }
}

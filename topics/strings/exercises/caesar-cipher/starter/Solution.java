public class Solution {

    /**
     * Encrypts the text by shifting each letter forward by the given amount.
     * Non-letter characters are left unchanged. Case is preserved.
     *
     * @param text  the text to encrypt
     * @param shift the number of positions to shift (positive)
     * @return the encrypted text
     */
    public static String encrypt(String text, int shift) {
        // TODO: Shift each letter forward by 'shift' positions
        return "";
    }

    /**
     * Decrypts the text by reversing a Caesar cipher with the given shift.
     *
     * @param text  the text to decrypt
     * @param shift the shift that was used to encrypt
     * @return the decrypted text
     */
    public static String decrypt(String text, int shift) {
        // TODO: Reverse the encryption
        return "";
    }
}

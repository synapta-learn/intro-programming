public class WordFrequency {

    /**
     * Returns the most frequent word in the sentence (case-insensitive).
     * Words are separated by spaces. If there is a tie, returns the
     * word that appears first. Returns "" for empty input.
     *
     * @param sentence the input sentence
     * @return the most frequent word (lowercase)
     */
    public static String mostFrequentWord(String sentence) {
        // TODO: Find and return the most frequent word
        return "";
    }
}

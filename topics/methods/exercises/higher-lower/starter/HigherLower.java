public class HigherLower {

    /**
     * Compares a guess against the secret number.
     * Returns "higher" if secret > guess, "lower" if secret < guess, "correct" if equal.
     */
    public static String checkGuess(int secret, int guess) {
        // TODO: Compare guess to secret
        return "";
    }

    /**
     * Returns how many guesses it took to find the secret.
     * Returns -1 if the secret was never guessed.
     */
    public static int countGuesses(int secret, int[] guesses) {
        // TODO: Loop through guesses, use checkGuess, count until correct
        return 0;
    }

    /**
     * Returns true if guess is within [min, max] inclusive.
     */
    public static boolean isValidGuess(int guess, int min, int max) {
        // TODO: Check if guess is in range
        return false;
    }

    /**
     * Returns the optimal midpoint guess for a range.
     */
    public static int bestGuess(int min, int max) {
        // TODO: Return the midpoint
        return 0;
    }
}

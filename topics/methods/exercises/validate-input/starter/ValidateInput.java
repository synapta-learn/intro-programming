public class ValidateInput {

    /**
     * Returns true if n is greater than 0.
     */
    public static boolean isPositive(int n) {
        // TODO: Check if n is positive
        return false;
    }

    /**
     * Returns true if n is between min and max (inclusive).
     */
    public static boolean isInRange(int n, int min, int max) {
        // TODO: Check if n is in the range [min, max]
        return false;
    }

    /**
     * Returns true if s is not null and has length > 0.
     */
    public static boolean isNonEmpty(String s) {
        // TODO: Check if s is non-null and non-empty
        return false;
    }
}

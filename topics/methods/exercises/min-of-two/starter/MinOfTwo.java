public class MinOfTwo {

    /**
     * Returns the smaller of two integers.
     */
    public static int min(int a, int b) {
        // TODO: Return the smaller int
        return 0;
    }

    /**
     * Returns the smaller of two doubles.
     */
    public static double min(double a, double b) {
        // TODO: Return the smaller double
        return 0;
    }

    /**
     * Returns the shorter of two strings.
     * If equal length, returns the first string.
     */
    public static String min(String a, String b) {
        // TODO: Return the shorter string (by length)
        return "";
    }
}

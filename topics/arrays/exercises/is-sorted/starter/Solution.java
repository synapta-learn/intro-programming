public class Solution {

    /**
     * Returns true if the array is sorted in ascending (non-decreasing) order.
     * Empty and single-element arrays are considered sorted.
     *
     * @param numbers the array to check
     * @return true if sorted in ascending order
     */
    public static boolean isSorted(int[] numbers) {
        // TODO: Check if each element is <= the next element
        return false;
    }
}

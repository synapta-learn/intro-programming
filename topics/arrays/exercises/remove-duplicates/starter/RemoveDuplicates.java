public class RemoveDuplicates {

    /**
     * Returns a new array with duplicates removed, preserving order of first appearance.
     *
     * @param numbers the input array
     * @return a new array with only unique elements
     */
    public static int[] removeDuplicates(int[] numbers) {
        // TODO: Remove duplicates while preserving order
        // Hint: You might want a helper approach — first collect unique values
        // into a temporary array, then copy into a correctly-sized result.
        return new int[0];
    }
}

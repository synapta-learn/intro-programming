public class Solution {

    /**
     * Returns a new array with elements rotated k positions to the right.
     *
     * @param numbers the array to rotate
     * @param k the number of positions to rotate right
     * @return a new rotated array
     */
    public static int[] rotate(int[] numbers, int k) {
        // TODO: Create and return a new array rotated k positions to the right
        return new int[0];
    }
}

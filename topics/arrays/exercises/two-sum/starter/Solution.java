public class Solution {

    /**
     * Finds two indices whose elements sum to target.
     * Returns the indices as a 2-element array, or an empty array if no pair exists.
     *
     * @param numbers the array to search
     * @param target the target sum
     * @return array of two indices, or empty array
     */
    public static int[] twoSum(int[] numbers, int target) {
        // TODO: Find two elements that add up to target
        return new int[0];
    }
}

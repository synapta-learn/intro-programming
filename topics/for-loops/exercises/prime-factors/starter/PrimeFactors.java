public class PrimeFactors {

    /**
     * Returns an array of prime factors of n in ascending order.
     * Repeated factors appear multiple times.
     *
     * @param n a positive integer
     * @return array of prime factors
     */
    public static int[] primeFactors(int n) {
        // TODO: Find all prime factors of n
        // Hint: You may want to use a temporary array and then copy
        // the relevant portion into a correctly-sized result.
        return new int[0];
    }
}

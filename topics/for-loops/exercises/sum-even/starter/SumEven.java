public class SumEven {

    /**
     * Returns the sum of all even numbers from 1 to n (inclusive).
     *
     * @param n the upper bound (inclusive)
     * @return sum of even numbers in the range [1, n]
     */
    public static int sumEven(int n) {
        // TODO: Use a for loop to sum all even numbers from 1 to n
        return 0;
    }
}

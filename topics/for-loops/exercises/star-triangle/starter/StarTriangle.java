public class StarTriangle {

    /**
     * Returns a right triangle of stars with n rows.
     * Row i has i stars (i starts at 1).
     * Each row ends with a newline.
     *
     * @param n number of rows
     * @return the triangle as a string
     */
    public static String starTriangle(int n) {
        // TODO: Use nested for loops to build the triangle
        return "";
    }
}

public class Solution {

    /**
     * Returns the nth row of Pascal's triangle (0-indexed).
     *
     * @param n the row number (0-indexed)
     * @return the nth row as an array
     */
    public static int[] pascalRow(int n) {
        // TODO: Build Pascal's triangle row by row until you reach row n
        return new int[0];
    }
}

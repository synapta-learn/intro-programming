public class PowerOfTwo {

    /**
     * Returns the smallest power of 2 that is >= n.
     * Assumes n >= 1.
     *
     * @param n a positive integer
     * @return the smallest power of 2 >= n
     */
    public static int powerOfTwo(int n) {
        // TODO: Start at 1 and keep doubling until >= n
        return 0;
    }
}

public class CollatzSteps {

    /**
     * Returns the number of steps to reach 1 in the Collatz sequence.
     * If even: n = n / 2. If odd: n = 3 * n + 1.
     * Assumes n >= 1.
     *
     * @param n the starting number
     * @return the number of steps to reach 1
     */
    public static int collatzSteps(int n) {
        // TODO: Count steps until n reaches 1
        return 0;
    }
}

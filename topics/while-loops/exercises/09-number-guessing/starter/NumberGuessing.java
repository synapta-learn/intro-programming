public class NumberGuessing {

    /**
     * Simulates a binary search guessing game.
     * Returns how many guesses it takes to find the target
     * in the range 1 to n, always guessing the midpoint.
     *
     * @param n the upper bound of the range (1 to n)
     * @param target the secret number to find
     * @return the number of guesses needed
     */
    public static int guessCount(int n, int target) {
        // TODO: Implement binary search and count the guesses
        return 0;
    }
}

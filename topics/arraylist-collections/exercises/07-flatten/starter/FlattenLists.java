import java.util.ArrayList;

public class FlattenLists {

    /**
     * Flattens a list of lists into a single list.
     * Elements appear in the order they exist in the nested structure.
     *
     * @param nested the list of lists to flatten
     * @return a single flat list containing all elements
     */
    public static ArrayList<Integer> flatten(ArrayList<ArrayList<Integer>> nested) {
        // TODO: Iterate through each inner list and collect all elements
        return new ArrayList<>();
    }
}

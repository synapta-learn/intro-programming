import java.util.ArrayList;

public class FilterAndTransform {

    /**
     * Filters the list to keep only even numbers, then doubles each value.
     * Returns a new list without modifying the original.
     *
     * @param numbers the input list
     * @return a new list with only even numbers, each doubled
     */
    public static ArrayList<Integer> filterAndDouble(ArrayList<Integer> numbers) {
        // TODO: Filter to even numbers and double each one
        return new ArrayList<>();
    }
}

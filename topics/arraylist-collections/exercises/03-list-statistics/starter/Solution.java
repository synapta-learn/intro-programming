import java.util.ArrayList;

public class Solution {

    /**
     * Computes statistics for the given list of integers.
     * Returns a double[4]: {min, max, sum, average}.
     * Returns {0, 0, 0, 0} if the list is empty.
     *
     * @param numbers the list of integers
     * @return a double array with min, max, sum, and average
     */
    public static double[] statistics(ArrayList<Integer> numbers) {
        // TODO: Handle empty list
        // TODO: Find min, max, sum, and compute average
        return new double[]{0, 0, 0, 0};
    }
}

import java.util.ArrayList;

public class Solution {

    /**
     * Creates an ArrayList containing all elements from the given array.
     *
     * @param items the array of strings to add
     * @return a new ArrayList containing the items
     */
    public static ArrayList<String> createList(String[] items) {
        // TODO: Create a new ArrayList and add all items from the array
        return new ArrayList<>();
    }

    /**
     * Removes the element at the given index and returns it.
     * Returns null if the index is out of bounds.
     *
     * @param list  the list to remove from
     * @param index the index to remove
     * @return the removed element, or null if index is invalid
     */
    public static String removeByIndex(ArrayList<String> list, int index) {
        // TODO: Check bounds, then remove and return the element
        return null;
    }

    /**
     * Removes the first occurrence of the given value.
     *
     * @param list  the list to remove from
     * @param value the value to remove
     * @return true if the value was found and removed
     */
    public static boolean removeByValue(ArrayList<String> list, String value) {
        // TODO: Remove the value and return whether it was found
        return false;
    }

    /**
     * Converts the ArrayList to a String array.
     *
     * @param list the list to convert
     * @return a String array containing all elements
     */
    public static String[] toArray(ArrayList<String> list) {
        // TODO: Convert the list to an array and return it
        return new String[0];
    }
}

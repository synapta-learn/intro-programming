import java.util.ArrayList;

public class UniqueElements {

    /**
     * Returns a new ArrayList containing only the unique elements
     * from the input list, preserving the order of first occurrence.
     *
     * @param list the input list (not modified)
     * @return a new list with duplicates removed
     */
    public static ArrayList<String> uniqueElements(ArrayList<String> list) {
        // TODO: Build a new list with only unique elements
        return new ArrayList<>();
    }
}

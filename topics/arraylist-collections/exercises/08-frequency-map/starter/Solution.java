import java.util.ArrayList;

class FrequencyCounter {

    // TODO: Declare parallel ArrayLists for keys and counts

    /**
     * Analyzes the given list and counts occurrences of each unique item.
     *
     * @param items the list to analyze
     */
    public FrequencyCounter(ArrayList<String> items) {
        // TODO: Build the frequency data using parallel lists
    }

    /**
     * Returns how many times the given item appeared.
     *
     * @param item the item to look up
     * @return the count, or 0 if not found
     */
    public int countOf(String item) {
        // TODO: Find the item in keys and return its count
        return 0;
    }

    /**
     * Returns the most common item. Ties broken by first occurrence.
     *
     * @return the most frequent item, or null if empty
     */
    public String mostCommon() {
        // TODO: Find the item with the highest count
        return null;
    }

    /**
     * Returns the number of unique items.
     *
     * @return the unique item count
     */
    public int uniqueCount() {
        // TODO: Return the number of unique keys
        return 0;
    }
}

public class Solution {
    public double x;
    public double y;

    /**
     * Returns the Euclidean distance from this point to another point.
     *
     * @param other the other point
     * @return the distance between the two points
     */
    public double distanceTo(Solution other) {
        // TODO: Calculate distance using sqrt((x2-x1)^2 + (y2-y1)^2)
        return 0;
    }

    /**
     * Moves this point by adding dx to x and dy to y.
     *
     * @param dx the amount to add to x
     * @param dy the amount to add to y
     */
    public void translate(double dx, double dy) {
        // TODO: Move the point
    }

    /**
     * Returns the distance from this point to the origin (0, 0).
     *
     * @return the distance to origin
     */
    public double distanceToOrigin() {
        // TODO: Calculate distance to (0, 0)
        return 0;
    }
}
